{
  "name": "fassungslos",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve",
    "bundle": "./node_modules/.bin/esbuild index.ts --bundle --outfile=./dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.23.1",
    "serve": "^14.2.3"
  }
}
